[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone-livechat"
version = "0.0.5"
author = "xensidewtf"
description = "Send minecraft chat to discord using webhook."

[project.entry-points."endstone"]
playerinfo = "endstone_livechat:livechat"
